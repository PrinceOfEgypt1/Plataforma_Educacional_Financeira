[pytest]
# F2 — testes do domínio puro. Rootdir é backend/, para que `import app.*`
# resolva contra o pacote `app/` irmão de `tests/`.
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --strict-config
filterwarnings =
    error
